# SOULHEALTH AI中医辨证溯源APP — 前端开发规格书

> 后端已全部完成（FastAPI），本文档供前端开发人员对接使用。
> 技术栈建议：UniApp（安卓+iOS）或 Web（Vue3/React均可）

---

## 一、后端启动方式

```bash
cd d:\桌面\ai2
uvicorn pipeline:app --host 0.0.0.0 --port 8000
```

- 健康检查：`GET /health` → `{"status":"ok","version":"pipeline/1.0"}`
- 基础URL：`http://localhost:8000`

---

## 二、API 接口清单

### 2.1 获取症状问诊量表
```
GET /api/v1/questionnaire?sex=M
```
**响应**：
```json
{
  "version": "0.1.0",
  "total": 17,
  "instruction": "请对每项症状按0-10分打分...",
  "dimensions": [
    {"key": "入睡困难", "label": "睡眠质量", "prompt": "近一周入睡困难程度", "category": "睡眠"},
    {"key": "食欲差", "label": "食欲", "prompt": "近一周食欲减退程度", "category": "消化"},
    ...
  ]
}
```
> 男性17项，女性20项（多3项经期维度）。前端按 `category` 分组展示。

### 2.2 生成完整报告（核心接口）
```
POST /api/v1/full_report
Content-Type: application/json
```
**请求体**：
```json
{
  "patient": {
    "age": 34,
    "sex": "F",
    "weight_kg": 52,
    "height_cm": 162,
    "pregnant": false,
    "allergies": ["青霉素"]
  },
  "lab_raw": [
    {"name_raw": "谷丙转氨酶(ALT)", "value": 68, "unit": "U/L"},
    {"name_raw": "甘油三酯", "value": 2.8, "unit": "mmol/L"},
    {"name_raw": "血红蛋白", "value": 95, "unit": "g/L"}
  ],
  "tongue": {
    "coat_thickness": 1,
    "greasy_index": 0.2
  },
  "face": {},
  "symptoms": {
    "怕冷": 3,
    "疲劳": 5,
    "情绪抑郁": 6,
    "胀痛": 7
  },
  "current_drugs": ["华法林"]
}
```

**响应结构**（精简版，完整字段见下方§三）：
```json
{
  "version": "pipeline/1.0",
  "generated_at": "2026-08-10T18:00:00",
  "patient": {...},
  "lab_result": {
    "total_count": 3,
    "abnormal_count": 3,
    "indicators": [...],
    "derived": {"liver_grade": 1, "renal_grade": 0}
  },
  "syndrome_result": {
    "primary": "肝郁",
    "ranked": ["肝郁","脾虚","痰湿",...],
    "percent": {"肝郁":46.0, "脾虚":22.0, ...}
  },
  "dosage_result": {
    "status": "OK",
    "base_formula": {"name":"柴胡疏肝散", "book":"《景岳全书》"},
    "prescription": [{"herb":"柴胡","dose_g":7.3,"role":"君"}, ...],
    "total_g": 59.9,
    "signoff": "处方建议（待执业中医师签发）"
  },
  "drug_interaction": {
    "should_block": true,
    "conflicts": [{"drug":"华法林","herb":"丹参","level":"forbid","consequence":"出血风险"}]
  },
  "explain_summary": {"has_d1":true, "has_d2":true, "has_d3":true, "has_d4":true},
  "toxicology": {"status":"OK", "conclusion":{"pass":true, "text":"五项安全鉴定均通过"}},
  "lifestyle": {"status":"OK", "diet_count":10},
  "markdown": {
    "explain": "# 组方解释报告（四维全维度溯源）\n...",
    "toxicology": "# 个人专属无毒安全鉴定报告\n...",
    "lifestyle": "# 个人专属健康干预方案\n..."
  }
}
```

> **关键**：`markdown` 字段里有三份渲染好的 Markdown 报告，前端可直接用 Markdown 渲染库展示。

---

## 三、页面结构（固定7个页面）

### 页面1：首页
- 四个入口卡片：体检上传 / 舌面诊拍摄 / 问诊问卷 / 历史方案
- 随访提醒浮动通知条

### 页面2：体检报告上传
- 拍照/相册上传（OCR暂用手动录入过渡）
- 表单：指标名（下拉+搜索） + 数值 + 单位
- 支持的指标共 **25类**，分8组：

| 组别 | 指标 |
|------|------|
| 肝功能 | ALT, AST, GGT, 总胆红素, 直接胆红素, 白蛋白 |
| 肾功能 | 肌酐, 尿素氮, 尿酸 |
| 血脂 | 甘油三酯, 总胆固醇, LDL, HDL |
| 血糖 | 空腹血糖, 糖化血红蛋白 |
| 血常规 | 血红蛋白, 红细胞, 白细胞, 血小板, 血清铁蛋白 |
| 炎症 | C反应蛋白, 血沉 |
| 甲状腺 | 促甲状腺激素 |
| 凝血 | D二聚体, 纤维蛋白原 |

- 提交后显示异常分级结果（G0绿/G1黄/G2橙/G3红）

### 页面3：舌面诊拍摄
- 舌象：引导框 + 光线检测 + 质量校验不合格拦截
- 面部：引导框 + 正面无遮挡校验
- 拍完显示量化结果（舌色、苔质、面色等）

### 页面4：智能问诊
- 按 `GET /api/v1/questionnaire` 返回的维度动态渲染
- 每项一个 **滑块 0-10**，分类分组展示：

| 分组 | 症状项（男） |
|------|-------------|
| 睡眠 | 入睡困难 |
| 消化 | 食欲差、腹胀 |
| 二便 | 便溏、便秘、尿黄、夜尿多 |
| 寒热 | 怕冷、怕热 |
| 情志 | 情绪抑郁、烦躁易怒 |
| 体能 | 疲劳 |
| 汗出 | 自汗、盗汗 |
| 疼痛 | 刺痛固定、胀痛走窜 |
| 口味 | 口苦 |
| 经期(仅女) | 经期血块、经量少色淡、经前乳胀 |

### 页面5：个人信息 & 用药登记
- 基础信息：年龄、性别、身高、体重
- 特殊状态：是否怀孕/哺乳
- 过敏药材录入（多选标签）
- 当前服用西药录入（自由文本，支持多条）

### 页面6：调理组方详情页 ⭐核心页面
> **排版顺序固定，不可调整。** 调用 `POST /api/v1/full_report` 后渲染。

```
① 用户身体概况卡片
   - 体检异常指标摘要（颜色编码 G1黄/G2橙/G3红）
   - 中医证型雷达图（8证型占比百分比）
   - 主证型 + 兼夹证型 标签

② 精准组方清单卡片
   - 药材列表：名称 + 0.1g精确克数 + 君臣佐使角色标签
   - 药食同源标记（绿色小标）
   - 全方合计克数
   - ⚠ 如有中西药冲突，红色警告横幅

③ 模块1：宏观中医配伍原理
   → 渲染 markdown.explain 中 "维度一" 部分

④ 模块2：微观临床医学作用机制
   → 渲染 markdown.explain 中 "维度二" 部分

⑤ 模块3：每一味药材克重计算完整依据
   → 渲染 markdown.explain 中 "维度三" 部分
   → 每味药可展开/折叠

⑥ 模块4：反向排除说明
   → 渲染 markdown.explain 中 "维度四" 部分

⑦ 模块5：毒理安全鉴定报告
   → 渲染 markdown.toxicology
   → 五项分别打 ✅/⚠️ 图标

⑧ 模块6：权威溯源
   → 渲染 markdown.explain 中 "溯源附录" 部分

⑨ 服用方法 & 调理周期
   → dosage_result.usage 字段

⑩ 生活干预方案
   → 渲染 markdown.lifestyle
   → 饮食/运动/作息/情绪 分Tab展示
```

**BLOCKED 状态特殊处理**：
当 `dosage_result.status == "BLOCKED"` 时，不展示组方，显示拦截原因卡片：
- 标题（如"妊娠期活血禁区"）
- 为什么拦截（保护逻辑解释）
- 下一步建议（就医指引）

### 页面7：终身病历时间轴
- 纵向时间轴，按日期倒序
- 每条记录：图标(体检📋/舌诊👅/组方💊) + 日期 + 摘要
- 点击展开详情
- 顶部筛选器：按类型过滤
- 导出按钮（调用后端导出Markdown/PDF）

---

## 四、状态码 & 错误处理

| dosage_result.status | 含义 | 前端行为 |
|---------------------|------|---------|
| `OK` | 正常出方 | 展示完整10项报告 |
| `BLOCKED` | 安全拦截 | 仅展示拦截原因卡片，不展示组方 |

| drug_interaction.should_block | 含义 | 前端行为 |
|------|------|---------|
| `true` | 严重中西药冲突 | 红色警告横幅，建议就医 |
| `false` + has_warnings | 轻度注意 | 黄色提示条 |

---

## 五、UI设计建议

- **风格**：水墨国风 + 现代卡片式布局，深色/浅色双模式
- **主色**：墨绿 `#2D5F4B` + 金色 `#C9A86C` + 米白 `#F5F0E8`
- **字体**：思源宋体（标题）+ 思源黑体（正文）
- **安全色**：正常绿 `#4CAF50` / 轻度黄 `#FFC107` / 中度橙 `#FF9800` / 重度红 `#F44336`
- **关键交互**：
  - 证型雷达图（echarts/chart.js）
  - 指标趋势折线图
  - 症状滑块 0-10
  - Markdown 渲染（推荐 marked.js + highlight.js）
  - 药材卡片展开/折叠动画

---

## 六、数据流示意

```
用户操作                         API调用                          后端引擎
─────────                       ──────                          ────────
录入基础信息 ──────────────────┐
上传体检指标 ──────────────────┤
拍摄舌面诊照片 ────────────────┤
填写症状问卷（滑块0-10）───────┤
录入当前西药 ──────────────────┘
        │
        ▼
  点击「生成方案」
        │
        ▼
                    POST /api/v1/full_report ──→ 指标解析(批1)
                         (一次请求)                  ↓
                              │               证型辨证(批3)
                              │                  ↓
                              │              中西药校验
                              │                  ↓
                              │              精准组方(批4)
                              │                  ↓
                              │             四维解释(批5)
                              │                  ↓
                              │             毒理报告(模7)
                              │                  ↓
                              │             生活干预(模8)
                              │
                    ←── 完整JSON响应 ──────────────┘
        │
        ▼
  渲染10项固定结构报告页
```

---

## 七、症状key值速查表（前端提交用）

前端提交 `symptoms` 时，key 必须使用以下固定值：

```
入睡困难 | 食欲差 | 腹胀 | 便溏 | 便秘 | 尿黄 | 夜尿多
怕冷 | 怕热 | 情绪抑郁 | 烦躁易怒 | 疲劳 | 自汗 | 盗汗
刺痛固定 | 胀痛走窜 | 口苦
经期血块 | 经量少色淡 | 经前乳胀（仅sex=F时提交）
```

每个 key 对应整数值 0-10。

---

## 八、前端不需要关心的事

以下逻辑全部在后端完成，前端只需提交原始数据、渲染返回结果：

- ❌ 指标异常分级算法（后端做）
- ❌ 证型权重计算（后端做）
- ❌ 0.1g剂量计算（后端做）
- ❌ 安全拦截判定（后端做）
- ❌ 十八反/十九畏检查（后端做）
- ❌ 中西药冲突检查（后端做）
- ❌ Markdown报告生成（后端做，前端只需渲染）
